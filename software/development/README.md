# Development

Compilers, assembler, linker, editors, debuggers and sprite tools.  The RT-11 compilers prompt with `*` for `output,listing=input` command lines; a Pascal build goes `R PAS1` → `R MACRO` → `R LINK` with `PASLIB.OBJ` (see the dev disks and `PASUSE.LST`).

