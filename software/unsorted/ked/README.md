# KED.SAV, the failed reads

Three reads of the collector's diskettes gave three different KED.SAV files, none of which runs: `baspasfor/` (the KryoFlux read of disk2), `disk1/` (the vvv104 read of disk1), `disk2/` (the vvv104 read of disk2).  The restored images of the same diskettes carry a running KED, shipped in `software/development/editors/`.  Kept to check what the damage looks like.

